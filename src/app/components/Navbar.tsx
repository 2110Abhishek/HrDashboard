"use client"; // ✅ Add this at the top

import React from "react";

const Navbar = () => {
  return (
    <nav className="bg-white shadow-md px-6 py-3 rounded-lg">
      {/* Empty Navbar (You can add content if needed) */}
    </nav>
  );
};

export default Navbar;
